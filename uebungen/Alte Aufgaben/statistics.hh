class statistics{  
public: 
static double mean(const std::vector<double>& v);
static double median (const std::vector <double> v);
static double standard_deviation(const std::std::vector<double> v; int k);
  };
